#ifndef PATH_RECONSTRUCT_H
#define PATH_RECONSTRUCT_H

#include <vector>
#include <utility> // for std::pair

std::vector<std::pair<int, int>> reconstruct_path(const std::vector<std::vector<int>>& dp);

#endif
